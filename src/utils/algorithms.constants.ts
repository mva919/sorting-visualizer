import { Algorithm } from '../context/AlgorithContext.types';

export const SORTING_ALGOS: Array<Algorithm> = [
  'bubble sort',
  'insertion sort'
];
export const MIN_ARRAY_SIZE = 10;
export const MAX_ARRAY_SIZE = 60;
